{
  "name": "torii-docs",
  "version": "1.0.0",
  "private": true,
  "description": "Torii Documentation",
  "scripts": {
    "dev": "wrangler dev",
    "build": "mdbook build",
    "deploy": "mdbook build && wrangler deploy",
    "mdbook:install": "cargo install mdbook"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
